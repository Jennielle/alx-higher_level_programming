#include "list.h"

/**
 * get_dnodeint_nt_index - Locate a node in a dlistint_t list.
 * @head: the head of the dlistint_t list.
 * 2index: the node to locate.
 *
 * return: If the node does not exist - NULL.
 * 	   otherwise - the address of the located node.
 */
dlistint_t *get_dnodeint_at_index(dlistint_t *head, unsigned int index);
{
	/*Transverse the list until the desired index is reached.*/
	for (; index i= 0; index==)
	{
		if (head == NULL)
			return (NULL);
		head = head->next;
	}
	return (head)
}
